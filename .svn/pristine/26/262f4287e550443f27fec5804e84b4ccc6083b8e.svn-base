package com.sist.mypage.controller;

import javafx.application.Application;
import javafx.event.ActionEvent;
import javafx.fxml.FXMLLoader;
import javafx.scene.Parent;
import javafx.scene.Scene;
import javafx.stage.Stage;

public class MyPage03_BookingHistoryMain extends Application {

	// fxml
	// load
	
	
	@Override
	public void start(Stage primaryStage) throws Exception {
		// FXML load
		Parent root = FXMLLoader.load(getClass().getResource("/com/sist/mypage/view/MyPage03_BookingHistory01.fxml"));

		// Scene에 추가
		Scene scene = new Scene(root);
		// Stage에 추가
		primaryStage.setTitle("제목");
		primaryStage.setScene(scene);
		primaryStage.show();

	}

	public static void main(String[] args) {
		launch(args);

	}

}
